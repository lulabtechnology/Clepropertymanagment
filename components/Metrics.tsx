"use client";
/**
 * Placeholder para métricas (GA / GTM).
 * Cuando tengas el ID, monta tu script aquí.
 */
import { useEffect } from "react";

export default function Metrics() {
  useEffect(() => {
    // Ejemplo: window.gtag('config', 'G-XXXXXXX');
  }, []);
  return null;
}
